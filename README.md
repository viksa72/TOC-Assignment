# TOC-Assignment
A Turing Machine simulator that processes input on an infinite tape using rules and states, starting from q0. It visually shows each step as the head reads, writes, and moves until halting.
